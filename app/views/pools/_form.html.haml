= form_with(model: pool, local: true) do |form|
  - if pool.errors.any?
    #error_explanation
      %h2
        = pluralize(pool.errors.count, "error")
        prohibited this pool from being saved:
      %ul
        - pool.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :pool_name
  .actions
    = form.submit
